extends layout

block layout-content

  if projects.length
      div.project
        each project in projects
          h2 project.title
          p project.description
          p project.date
          p project.owner
  else
    p No projects yet :(
