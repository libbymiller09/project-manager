extends layout

block layout-content
  div.View.WelcomeView
    h1 The Project Manager
    div.NavButtons
      if isAuthenticated
        a(href='/user')
          div.NavButton User Profile Button!
      else
        a(href='/login')
          div.NavButton Log in!
